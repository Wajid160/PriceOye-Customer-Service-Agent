[project]
name = "priceoye"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Wajid160", email = "wajidjaved160@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "dotenv-python>=0.0.1",
    "openai-agents>=0.0.13",
]

[project.scripts]
priceoye = "priceoye:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
